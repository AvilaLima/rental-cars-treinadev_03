<h1>Categorias dos Carros</h1>
<%= link_to 'Registrar nova categoria', new_carcategory_path%>
<div class="notice">
  <%= notice %>
</div>
<ul>
    <% @carcategories.each do |cc|%>
        <li><%= link_to "#{cc.name}: #{cc.daily_rate}", cc %></li>
    <%end%> 
</ul>
<%= link_to 'Voltar', root_path %>