<h1>Categorias dos Carros</h1>
<div class="notice">
  <%= notice %>
</div>
<ul>
    <% @carcategories.each do |cc|%>
        <li><%= link_to  cc.name, cc %></li>
    <%end%> 
</ul>
<%= link_to 'Voltar', root_path %>